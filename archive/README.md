# Project Archive

This directory contains superseded material retained for history or inspiration. Nothing here has current product, architecture, roadmap, or design authority.

- `docs/` — superseded documents and implementation handoffs.
- `prototypes/` — deprecated product explorations.

Active work belongs in `docs/`, `prototypes/current/`, `reviews/`, or `atlases/`. Git history preserves deleted duplicate artifacts.
