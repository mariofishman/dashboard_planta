# Deprecated dashboard prototype

`index.html` is deprecated and has no current product authority. It remains only as a historical reference or possible source of future inspiration.

The valid dashboard is `../chat-list-review/dashboard.html`.
