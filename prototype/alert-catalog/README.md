# Alert catalog prototypes

- `final/index.html` is the approved browser publication.
- `v11/index.html` records the approved E05 review iteration.
- `v1/` through `v10/` are deprecated historical review iterations retained only for traceability.
- `shared/` contains the CSS and JavaScript used by the review history and final publication where applicable.

Current product authority begins at `docs/product_definition.md` in the repository root.
