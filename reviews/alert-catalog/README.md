# Alert Catalog Review

- `publication/index.html` is the approved browser publication.
- `iterations/v12/index.html` records the approved E02–E05 snapshot-source review.
- `iterations/v11/index.html` records the approved E05 review iteration.
- `iterations/v1/` through `v10/` are historical review iterations retained for traceability.
- `shared/` contains the CSS and JavaScript used by the review history and final publication where applicable.

The publication is a review surface, not independent product authority. Current alert authority is `docs/product/alert_catalog.md`.
