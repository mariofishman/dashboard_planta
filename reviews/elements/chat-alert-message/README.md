# Alert object inside a chat message

Approved interactive review: <http://127.0.0.1:4177/chat-alert-message/>

Run the shared server using [`../README.md`](../README.md).

The committed defaults reproduce the approved selection. Browser-local experiments may still change the live matrix without changing `final-specification.md`.
