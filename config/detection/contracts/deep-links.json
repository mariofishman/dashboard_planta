{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "contractVersion": "2.1.0",
  "status": "discovered-pending-phase10-validation",
  "source": {
    "catalogVersion": 5,
    "observedAt": "2026-07-23T14:43:34Z",
    "evidence": "docs/integrations/emusasoft/evidence/database_and_mcp_compatibility_audit_2026-07-23.md",
    "baseUrl": "https://erp-web.apps.emusa.dev",
    "productExamplesConfirmedOn": "2026-07-23"
  },
  "destinations": {
    "workOrder": {
      "requiredIdentifiers": ["workOrderId"],
      "activeTemplate": "/work-orders/:workOrderId",
      "closedTemplate": "/work-orders/closed/:workOrderId",
      "enabled": false,
      "fallback": "display the work-order identifier and available evidence inside Monitor"
    },
    "workOrderDetail": {
      "requiredIdentifiers": ["workOrderId", "itemType", "itemId"],
      "activeTemplate": "/work-orders/:workOrderId/detail/:itemType/:itemId",
      "closedTemplate": "/work-orders/closed/:workOrderId/detail/:itemType/:itemId",
      "enabled": false,
      "fallback": "display the related identifiers and available evidence inside Monitor"
    },
    "materialReservation": {
      "requiredIdentifiers": ["workOrderId"],
      "template": "/work-orders/:workOrderId/process/reservation",
      "manualOnly": true,
      "enabled": false,
      "fallback": "display the work-order and material identifiers and available evidence inside Monitor"
    }
  },
  "enablementRequirements": [
    "Environment base URL is supplied through configuration.",
    "Active and closed route selection is confirmed.",
    "Allowed itemType, itemId, and action values are documented.",
    "Authorization behavior and compatibility policy are approved.",
    "Representative authorized and unauthorized browser tests pass."
  ],
  "rules": [
    "A discovered route is not enabled until every Phase 10 requirement passes.",
    "Never infer a browser route from a GraphQL operation name.",
    "Identifiers and evidence remain the fallback when navigation is unavailable."
  ]
}
